{
  "name": "vadapav-downloader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "electron-packager . Vadapav downloader --platform=win32 --out=dist --ignore='^/dist$' --prune --icon=favicon-32x32.ico --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.2.2"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
