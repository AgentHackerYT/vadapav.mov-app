{
  "name": "vpmov",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-win": "electron-packager . Vadapav downloader --platform=win32 --out=dist --ignore='^/dist$' --prune --icon=favicon.ico --overwrite",
    "build-linux": "electron-packager . Vadapav downloader --platform=linux --out=dist --ignore='^/dist$' --prune --icon=favicon.ico --overwrite",
    "build": "npm run build-win && npm run build-linux"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^29.1.5"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "vlc-simple-player": "^0.5.1"
  }
}
