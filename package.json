{
  "name": "vadapav-downloader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-win": "electron-packager . Vadapav downloader --platform=win32 --out=dist --ignore='^/dist$' --prune --icon=favicon-32x32.ico --overwrite",
    "build-linux": "electron-packager . Vadapav downloader --platform=linux --out=dist --ignore='^/dist$' --prune --icon=favicon-32x32.ico --overwrite",
    "build": "npm run build-win && npm run build-linux"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.2.2"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
